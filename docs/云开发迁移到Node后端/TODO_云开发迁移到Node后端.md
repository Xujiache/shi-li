# TODO_云开发迁移到Node后端

## 一、必须补充的配置

| 项目 | 位置 | 当前状态 | 说明 |
| --- | --- | --- | --- |
| 微信小程序 `WECHAT_APP_SECRET` | `node后端（新增）/.env` | 未填写 | 不填写则微信快捷登录只能保留代码，无法实测成功 |
| 小程序 API 地址 | `小程序端/最终（视力）/miniprogram/app.js` | 使用 `127.0.0.1` | 真机或局域网需改为实际可访问 IP/域名 |
| 后台代理目标 | `后台/art-lnb-master/.env.development` | 指向 `127.0.0.1:3000` | 如改端口或部署域名需同步调整 |

## 二、建议继续处理的事项

| 事项 | 当前状态 | 建议 |
| --- | --- | --- |
| 历史云函数目录保留 | 已保留 | 如确认不再需要，可做归档或删除 |
| 小程序示例页面 `pages/index`、`pages/example` | 仍为历史云开发示例 | 因未在 `app.json` 中启用，不影响运行；后续可删除 |
| 管理后台历史 CloudBase 工具文件 | 仍保留未使用文件 | 如需极致清理，可删除 `src/utils/cloudbase-auth.ts`、`src/utils/http/cloudbase.ts` 等历史参考文件 |
| 微信云开发旧说明文档 | 仍保留 | 可后续统一迁移到“历史文档”目录，避免混淆 |

## 三、推荐操作顺序

1. 先补 `WECHAT_APP_SECRET`
2. 启动 `node后端（新增）`
3. 启动管理后台并验证登录
4. 在微信开发者工具中将小程序 API 地址改成可访问地址后验证手机号登录
5. 再验证微信快捷登录

## 四、需要你确认的处理方式

请决定以下两项后续动作：

1. 是否要我继续帮你做“历史云开发参考文件归档/删除”清理。
2. 是否要我继续帮你把小程序和后台的本地地址改成你的局域网 IP，方便真机联调。
