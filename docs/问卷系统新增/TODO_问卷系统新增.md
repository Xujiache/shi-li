# TODO_问卷系统新增

## 一、后续建议

1. 继续补一个 `question-active.svg`，让底部 Tab 的“问卷”选中态图标更明显。
2. 在学校/班级字典与孩子档案编辑页中显式展示 `grade_name`，降低依赖 `class_name` 推断年级的风险。
3. 若后续需要专业量表结果分析，可新增问卷统计图表页。
4. 若后续题目配置更复杂，可继续做可视化跳题/显隐规则编排器。

## 二、真机联调提醒

- 小程序当前 API 地址仍为本地地址，如需真机调试需改成可访问 IP/域名。
- 微信快捷登录仍依赖 `WECHAT_APP_SECRET` 的真实配置。
