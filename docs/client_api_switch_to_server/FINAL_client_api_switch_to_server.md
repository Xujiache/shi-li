# FINAL_client_api_switch_to_server

## 1. 交付摘要
- 已将小程序端的全局 API 基地址与请求回退地址统一切换到 `https://api.gmxd.asia/api/v1`。
- 已将管理后台开发代理和生产环境 API 地址统一切换到 `https://api.gmxd.asia`。
- 已重新构建管理后台，确保 `dist` 产物不再携带旧的本地 IP 地址。

## 2. 结果说明
- 现在小程序源码层面已经统一指向 HTTPS API 域名，可直接进入联调验证阶段。
- 管理后台源码和生产构建也已完成地址切换，但正式服务器上线操作留待下一阶段执行。

## 3. 交付边界
- 已完成：客户端配置切换、后台构建同步、静态检查、文档同步。
- 未完成：管理后台正式上传与 Nginx/站点上线。
