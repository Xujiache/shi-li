# TASK_backend_server_upload

## 1. 原子任务拆分

### 任务 A：验证 SSH 可用性
- 输入契约：
  - 服务器 IP：`154.36.160.34`
  - 用户：`root`
- 输出契约：
  - 成功建立 SSH 会话，或明确阻塞原因
- 当前状态：
  - **已阻塞**
- 问题记录：
  - 22 端口可达
  - 但 `plink`、`ssh`、`ssh-keyscan` 均显示连接在握手初期被远端关闭

### 任务 B：上传后端代码
- 输入契约：
  - 任务 A 已通过
  - 本地后端目录可读
- 输出契约：
  - 服务器存在完整后端目录
- 当前状态：
  - 未开始（依赖任务 A）

### 任务 C：服务器环境检查
- 输入契约：
  - 任务 A 已通过
- 输出契约：
  - 明确服务器是否安装 Node / npm / MySQL
- 当前状态：
  - 未开始（依赖任务 A）

### 任务 D：初始化与启动
- 输入契约：
  - 任务 B、C 已完成
- 输出契约：
  - 后端可运行
- 当前状态：
  - 未开始（依赖任务 B、C）

## 2. 依赖关系
- 任务 A -> 任务 B
- 任务 A -> 任务 C
- 任务 B、C -> 任务 D

## 3. 阻塞说明
- 当前因 SSH 握手失败，整个远程执行链路暂停。
- 需要用户先修复服务器 SSH，或提供替代远程访问方式。
