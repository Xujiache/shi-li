# CONSENSUS_backend_server_upload

## 1. 当前共识
- 本次任务仅处理 `node后端（新增）` 上传服务器。
- 当前未进入“上传成功”阶段，原因不是密码错误，而是 SSH 握手异常。
- 在服务器 SSH 恢复可用前，无法继续执行真实上传与远程部署。

## 2. 验收标准
- 可通过 SSH 正常登录到 `root@154.36.160.34`
- 可将 `node后端（新增）` 上传到服务器目标目录
- 上传完成后可在服务器上看到后端源码目录和关键文件

## 3. 技术共识
- 优先通过 SSH/SFTP/PSCP 执行上传。
- 若 SSH 仍不可用，则需要用户提供替代上传通道或先修复 SSH 服务。
- SSH 恢复后，再继续：
  - 上传代码
  - 安装依赖
  - 初始化数据库
  - 启动后端服务
