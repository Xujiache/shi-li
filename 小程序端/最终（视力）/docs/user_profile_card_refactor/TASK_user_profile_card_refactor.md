# TASK_user_profile_card_refactor: 个人中心头像信息卡大厂风重构

## 1. 任务拆分

### T1: 现有结构与样式审查
- **输入契约**: `pages/user/index` 的 WXML/WXSS。
- **输出契约**: 明确可改动的 `profile-card` 子范围与样式要点。
- **实现约束**: 仅限圈选区域。
- **依赖关系**: 无。

### T2: 样式重构（主任务）
- **输入契约**: DESIGN 与 CONSENSUS 文档。
- **输出契约**: `profile-card` 及其子元素样式升级。
- **实现约束**: 不改功能、不改数据绑定、不加依赖。
- **依赖关系**: 依赖 T1。

### T3: 验收与记录
- **输入契约**: 更新后的 WXML/WXSS。
- **输出契约**: `ACCEPTANCE` 文档完成状态。
- **实现约束**: 验收点必须可测试。
- **依赖关系**: 依赖 T2。

## 2. 任务依赖图

```mermaid
flowchart LR
  T1 --> T2 --> T3
```
