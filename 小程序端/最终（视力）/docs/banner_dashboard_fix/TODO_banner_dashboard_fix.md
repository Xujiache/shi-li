# TODO_banner_dashboard_fix

## 部署/验证待办（你可以直接按此清单走）

### 1) 云函数重新部署（必须）

- [ ] 在微信开发者工具「云开发」中上传并部署：
  - `cloudfunctions/admin_manager`
  - （若你也改过其它云函数请一并部署）`cloudfunctions/data_manager`

### 2) 后台管理端发布

- [ ] 重新构建并发布 `art-lnb-master`（本地已通过 `pnpm build`）
- [ ] 确认后台轮播图管理页能看到并编辑“主标题/副标题”字段

### 3) 小程序端验证

- [ ] 小程序重新编译/预览
- [ ] 首页轮播图：确认图片可更新，且主/副标题显示正常
- [ ] 数据看板：确认首屏内容宽度与首页一致

### 4) 数据清理建议（可选）

- [ ] 检查 `banners.image_url` 是否存在误填 `/pages/...` 的脏数据
  - 建议在后台逐条修正为上传图片产生的 `cloud://...` FileID 或可访问的 `https://...` 地址

