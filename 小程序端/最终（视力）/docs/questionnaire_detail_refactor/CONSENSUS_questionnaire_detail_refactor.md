# CONSENSUS_questionnaire_detail_refactor

## 1. 最终共识
- 详情页将升级为与问卷首页一致的蓝色渐变头图 + 白色内容卡片布局
- 仅修复当前页面的重复 `Page(...)` 注册问题，不调整全局页面机制
- 所有状态文案、规则文案、统计文案统一在 `index.js` 中处理

## 2. 验收标准
- 页面能正常打开，不再出现 `Please do not register multiple Pages`
- 顶部展示问卷标题、简介、发布状态、剩余次数等核心信息
- 中部清晰展示填写对象、派发规则、提交规则
- 问卷结构与填写记录区具备更强的视觉层次
- “查看历史”“开始填写/继续填写”入口保持可用
- 不改变主题色，不使用 emoji 图标

## 3. 技术共识
- 删除 `index.js` 内重复的默认模板 `Page(...)`
- 删除 `index.wxml` 末尾残留的模板调试文本
- 增加详情页展示层派生数据：
  - 问卷状态文案
  - 剩余次数文案
  - 填写对象摘要
  - 分页/题目总数统计
  - 操作按钮文案与提示
